# Personal Makefile overrides. Copy to .env (git-ignored).
# For a project-local .venv:
# PIP=.venv/bin/pip
# Use uv as the package installer against system Python:
# PIP=uv pip --python python3 --system
